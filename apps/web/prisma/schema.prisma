datasource db {
  provider = "postgresql"
  url      = env("postgresql://postgres:postgres@localhost:5433/casting")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id        String   @id @default(cuid())
  email     String   @unique
  createdAt DateTime @default(now())
}