legacy-peer-deps=true
audit=false
fund=false
# Prisma relies on its postinstall script to download query engines.
# Allow install scripts to run so `pnpm prisma generate` works correctly.
ignore-scripts=false